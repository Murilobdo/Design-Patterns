namespace Builder.Models
{
    public enum TipoMassa
    {
        Normal = 0,
        Fina = 1,
        Grossa = 2
    }

    public enum TipoBorda
    {
        Normal = 0,
        Recheada = 1
    }

    public enum Tamanho
    {
        Pequena = 0,
        Media = 1,
        Grande = 2
    }
}